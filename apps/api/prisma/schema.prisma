generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Placeholder until the first real v3 data model migration is designed.
// The first real migration should be designed from the confirmed v3 data model.
